BufferedOutputStream

构造方法
----------------------------------------
public		BufferedOutputStream(OutputStream os)		//需要一输入流,缓冲区只是做到一个存储数据的区域，真正读写还是要用字节输出流

输入方法：
----------------------------------------
void		writer(byte[] bys)	//需要写入的数据

释放资源：
----------------------------------------
void		close()			//关闭缓冲区并且关闭输入流
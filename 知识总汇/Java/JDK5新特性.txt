增强for：	//用于替代迭代器，其实就是迭代器改版

String[] arr = ....

if(arr != null){		//增强for不能遍历为空的引用类型，必须判断，否则会报超出索引
for(String x : arr){		//格式： 目标类型  变量名 ： 目标变量
    syso(x);			//直接使用变量名即可，
  }
}

静态导入：
import static java.lang.Math.add;	//导入到方法级别，基本不会用

java.lang.Matn.add();			//多此一举
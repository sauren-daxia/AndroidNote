使用静态传递，步骤：

1、在目标界面定义public static 的属性

2、在原界面定义Intent意图对象

3、调用目标界面的静态属性，并为之赋值

4、启动意图

5、在目标界面直接调用属性即可，但前提属性不能为空









ps:在实现两个页面的跳转时，需要在配置文件对两个页面都进行绑定。也就是在<Appliction>标签中添加<action>